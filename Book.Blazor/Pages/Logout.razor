@page "/Logout"
@using Book.Blazor.IServices
@inject IAuthService _ser
@inject NavigationManager _navi
@code {
    protected override async Task OnInitializedAsync()
    {
        await _ser.Logout();
        _navi.NavigateTo("/");
    }
}
